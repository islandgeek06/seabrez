# Intelleson Browser — example environment (non-secret values only).
# Copy to .env for local development. AI API keys are NEVER stored here — they
# are entered in Settings → AI and encrypted with the OS secure store.

APP_NAME=Intelleson Browser
APP_ENV=development
DEFAULT_SEARCH_ENGINE=google
ENABLE_DEVTOOLS=true
LOG_LEVEL=info
